#cpp_additional_includes "
#include <cpp_type.h>
"

type c++::MyCppWrapper {
    #cpp_stack_owned "::CppType" (size = 8, align = 4);
}

extern "C++" {
    impl c++::MyCppWrapper {
        safe fn new(i32, i32) -> c++::MyCppWrapper;
        safe fn print(&self);
    }
}
